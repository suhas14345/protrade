rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Allow read/write for now to unblock dashboard (Suhas' shadow mode)
      // In production, we would restrict this to authenticated users.
      allow read, write: if true;
    }
  }
}
